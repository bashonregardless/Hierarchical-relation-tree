To work with data other than the default, change `const state = require('./path/to/file')`
line in flow-algo.js to one of the files in this folder, then produce data consumable by 
the program from file (see steps in README of root directory):

$ node flow-algo.js > data.json
